const getAll = async () => {
  const response = [];
  return response;
};

const getById = async (id) => {
  const response = [];
  return response;
};

const create = async (data) => {
  const response = [];
  return response;
};

const update = async (id, data) => {
  const response = [];
  return response;
};

const remove = async (id) => {
  const response = [];
  return response;
};

module.exports = {
  getAll,
  getById,
  create,
  update,
  remove
};
